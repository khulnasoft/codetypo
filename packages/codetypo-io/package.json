{
  "name": "codetypo-io",
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "version": "8.17.3",
  "description": "A library of useful I/O functions used across various codetypo tools.",
  "type": "module",
  "sideEffects": false,
  "module": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "!**/*.tsbuildInfo",
    "!**/__mocks__",
    "!**/*.test.*",
    "!**/*.spec.*",
    "!**/*.map"
  ],
  "scripts": {
    "build": "tsup-node",
    "build:watch": "tsup-node --watch",
    "watch": "pnpm build:watch",
    "clean": "shx rm -rf dist temp coverage \"*.tsbuildInfo\"",
    "clean-build": "pnpm run clean && pnpm run build",
    "coverage": "vitest run --coverage --pool=forks",
    "test-watch": "vitest",
    "test:build": "tsc -p .",
    "test": "pnpm test:build && vitest run --pool=forks"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/khulnasoft/codetypo.git",
    "directory": "packages/codetypo-io"
  },
  "keywords": [
    "codetypo"
  ],
  "author": "Jason Dent",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/khulnasoft/codetypo/labels/codetypo-io"
  },
  "homepage": "https://github.com/khulnasoft/codetypo/tree/main/packages/codetypo-io#readme",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "lorem-ipsum": "^2.0.8",
    "typescript": "~5.7.3",
    "vitest-fetch-mock": "^0.4.3"
  },
  "dependencies": {
    "@codetypo/codetypo-service-bus": "workspace:*",
    "@codetypo/url": "workspace:*"
  }
}
