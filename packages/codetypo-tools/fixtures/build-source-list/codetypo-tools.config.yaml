# yaml-language-server: $schema=./../../codetypo-tools.config.schema.json

targets:
  - name: source-list
    targetDirectory: ../../temp/builds/build-source-list
    sources:
      - listFile: ./sources.txt
    format: plaintext
