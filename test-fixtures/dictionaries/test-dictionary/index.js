// @ts-check

/** @type {import("@codetypo/codetypo-types").FileSettings} */
const config = {
    dictionaryDefinitions: [
        {
            name: 'test-dictionary',
            path: './words.txt',
        }
    ],
    dictionaries: ['test-dictionary'],
};

export default config;
