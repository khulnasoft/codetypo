{
  "name": "test-codetypo-pipe-esm",
  "version": "8.17.2",
  "description": "Pure testing package for @codetypo/codetypo-pipe. Its purpose is to verify @codetypo/codetypo-pipe dependencies.",
  "private": true,
  "bin": "./bin.mjs",
  "type": "module",
  "scripts": {
    "clean": "shx rm -rf dist temp coverage \"*.tsbuildInfo\"",
    "build": "pnpm run compile",
    "clean-build": "pnpm run clean && pnpm run build",
    "compile": "tsc -p .",
    "test": "pnpm test:unit && pnpm test:smoke",
    "test:smoke": "node ./bin.mjs",
    "test:unit": "vitest run"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@codetypo/codetypo-pipe": "workspace:*"
  },
  "main": "index.js",
  "keywords": [],
  "engines": {
    "node": ">=18"
  }
}
