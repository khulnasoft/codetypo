{
  "name": "test-codetypo-tools",
  "version": "8.17.2",
  "description": "Pure testing package for @codetypo/codetypo-tools.",
  "private": true,
  "scripts": {
    "clean": "shx rm -rf dist temp coverage \"*.tsbuildInfo\"",
    "build": "pnpm run compile && pnpm run build-dictionaries",
    "build-dev": "pnpm run compile-dev && pnpm run build-dictionaries",
    "clean-build": "pnpm run clean && pnpm run build",
    "build-dictionaries": "codetypo-tools-cli compile --no-compress -o dict src/php.txt src/companies.txt src/node.txt",
    "compile": "tsc -p .",
    "test-dist": "node dist",
    "test-codetypo": "codetypo -c codetypo.json \"src/*\"",
    "test": "pnpm run test-dist && pnpm run test-codetypo"
  },
  "author": "Md Sulaiman",
  "license": "MIT",
  "dependencies": {
    "@codetypo/codetypo-tools": "workspace:*",
    "codetypo": "workspace:*"
  },
  "main": "index.js",
  "keywords": []
}
